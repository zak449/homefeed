"use client";

/**
 * GeoFeedEnhancer — NO LONGER auto-redirects.
 * Location-based feed is now opt-in only (via SearchBar "Use Current Location").
 * This component is kept as a no-op to avoid breaking imports.
 */
export default function GeoFeedEnhancer() {
  return null;
}
